/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  output: "export",
  basePath: "/openstad-template-builder",
  assetPrefix: "/openstad-template-builder",
};

module.exports = nextConfig;
